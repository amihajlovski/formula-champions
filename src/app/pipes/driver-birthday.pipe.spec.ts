import { DriverBirthdayPipe } from './driver-birthday.pipe';

describe('DriverBirthdayPipe', () => {
  it('create an instance', () => {
    const pipe = new DriverBirthdayPipe();
    expect(pipe).toBeTruthy();
  });
});
